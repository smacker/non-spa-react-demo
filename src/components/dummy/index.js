import React from 'react';

export default function () {
    return (
        <div className="dummy">
            I'm a dummy component that renders only on server and never change.
        </div>
    );
}
